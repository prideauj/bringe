fastapi==0.115.12
uvicorn[standard]==0.34.2
httpx[http2]==0.28.1
# curl-cffi gives us a libcurl-backed HTTP client that impersonates real
# browser TLS fingerprints. brightonfringe.org sits behind Cloudflare Bot
# Management which 403s plain Python TLS, so the scraper uses this for
# its requests against brightonfringe.org. httpx stays for everything
# else (Nominatim, the review sites etc).
curl-cffi==0.7.4
beautifulsoup4==4.13.4
lxml==5.3.2
sqlalchemy==2.0.40
aiosqlite==0.21.0
pydantic==2.11.4
python-dotenv==1.1.0
apscheduler==3.11.0
